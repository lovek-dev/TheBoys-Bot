You are debugging a Discord bot that is deployed on Render and currently FAILS to start.

⚠️ CRITICAL CONTEXT:
- The bot worked in earlier commits
- It is now broken after multiple updates
- Previous fixes were unreliable
- You MUST find the exact breaking point OR rebuild completely

---

# 🎯 OBJECTIVE (NON-NEGOTIABLE)

1. Identify EXACT commit where Render startup broke
2. Prove WHY it broke (not assumptions)
3. Fix it permanently
4. If root cause is unclear → FULL REWRITE the bot cleanly for Render

---

# 🔬 STEP 1: STRICT COMMIT REPLAY (NO SHORTCUTS)

You MUST:

1. Go to the FIRST commit
2. Move commit-by-commit in chronological order

For EACH commit:

- Simulate startup behavior
- Check:
  ✔ Does bot start?
  ✔ Does it crash?
  ✔ Any missing env/config?
  ✔ Any breaking structural change?

---

## ⚡ IMPORTANT: USE BINARY SEARCH IF MANY COMMITS

If commits are large in number:

- Use binary search strategy:
  - Test middle commit
  - Narrow down working vs broken range
  - Find EXACT first failing commit efficiently

---

# 📍 STEP 2: IDENTIFY BREAKPOINT

You MUST output:

- ✅ Last working commit hash
- ❌ First broken commit hash

Then compare BOTH deeply.

---

# 🧠 STEP 3: ROOT CAUSE (NO GUESSING)

You MUST prove the issue using code-level evidence.

Check specifically:

### 1. ENTRY / STARTUP FLOW
- index.js / main.js changes
- client.login() issues
- incorrect imports/exports

### 2. RENDER REQUIREMENTS
- Missing or broken `start` script
- Not using `process.env.PORT`
- No web server (if required)
- Environment variables misuse

### 3. DEPENDENCIES
- Broken package versions
- Missing installs

### 4. ASYNC FAILURES
- Unhandled promise rejections
- Crashes before login

### 5. DISCORD.JS STRUCTURE
- Invalid intents
- Event handler not loading
- Partial config issues

---

# 🛠 STEP 4: FIX IMPLEMENTATION

Based on root cause:

### If SINGLE ISSUE:
→ Fix minimally and correctly

### If MULTIPLE ISSUES:
→ Stabilize architecture

### If CODEBASE IS UNSTABLE / CONFUSING:
→ ⚠️ MANDATORY FULL REWRITE

---

# 🔁 STEP 5: FULL REWRITE (IF NEEDED)

If you cannot confidently fix the issue:

You MUST rebuild the bot from scratch using:

✔ Clean Discord.js v14 structure  
✔ Proper command & event handler system  
✔ Safe async handling  
✔ Clear file structure  
✔ Environment-based config  
✔ Render-compatible startup  

---

# 🚀 STEP 6: RENDER HARD REQUIREMENTS

Final project MUST:

✔ Have correct `start` script  
✔ Use `process.env` for ALL secrets  
✔ Start without crashing  
✔ Log successful login  
✔ Optionally include Express server to keep service alive  

---

# 🧪 STEP 7: VALIDATION

Simulate startup:

- No crashes
- No missing modules
- Bot logs in successfully
- Stable execution

---

# 📦 STEP 8: FINAL OUTPUT

You MUST provide:

1. 📊 Commit analysis:
   - Last working commit
   - First broken commit

2. 🧠 Root cause with PROOF (code references)

3. 🛠 Fix explanation

4. 📁 Final code:
   - Either patched files OR full rewritten project

5. 🚀 Exact Render deployment steps

---

# ❗ HARD RULES

- DO NOT skip commits
- DO NOT assume — VERIFY
- DO NOT leave partial fixes
- DO NOT prioritize features over stability
- If debugging fails → FULL REWRITE is REQUIRED

---

# 💡 END GOAL

Bot MUST:
✅ Start on Render reliably  
✅ Stay online  
✅ Be clean, stable, production-ready  